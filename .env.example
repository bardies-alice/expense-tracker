# Copy this to .env.local and fill with real values
DATABASE_URL="postgresql://user:password@neon.tech/dbname?sslmode=require"

# --- Solo para docker-compose (crea un .env en la raíz para sobreescribir) ---
# POSTGRES_USER=expense_tracker
# POSTGRES_PASSWORD=expense_tracker
# POSTGRES_DB=expense_tracker
# CRON_SECRET=
